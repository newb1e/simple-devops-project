pipeline {
    agent any
    //{
    //    args '-p 1080:1080'
    //    }
    //}
    stages {
        stage('Build') {
            steps {
                sh 'echo hello'
                sh 'test.sh'
            }
        }
    }
}
